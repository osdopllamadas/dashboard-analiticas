version: '3'

services:
  web:
    container_name: crm-callcenter
    image: ghcr.io/osdopllamadas/dashboard-analiticas:latest
    restart: always
    ports:
      - "3000:3000"
    # environment: # Removed as per user request (no Supabase needed)

    # --- REDES ---
    networks:
      - default
